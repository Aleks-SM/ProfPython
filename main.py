from application.salary import calculate_salary
from application.db.models.base import BaseModel
from application.db.base import DBSession


if __name__ == '__main__':
    bd_test = BaseModel
    bd
    print(round(calculate_salary(), 2))